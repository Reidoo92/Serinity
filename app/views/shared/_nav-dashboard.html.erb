<div class="sidebar">
  <div class="sidebar-content">
    <nav class="sidebar-nav">
      <div class="left-navbar">
        <div class="pp-avatar">
          <%#= image_tag "https://kitt.lewagon.com/placeholder/users/ssaunier", class: "avatar" %>

          <%= link_to user_path(current_user), class: "link-current-user" do %>

            <% if current_user.photo.attached?  %>
              <%= cl_image_tag current_user.photo.key, class: "avatar", crop: :fill %>
            <% else %>
              <%= image_tag("pp_default.png", class: "avatar") %>
            <% end %>

            <%= "Dr. #{current_user.last_name.upcase}" %>
          <% end %>
        </div>

        <div class="nav-dashboard">

          <div class="label-link-dashboard">
            <%= link_to "Dashboard", root_path %>
          </div>

          <div class="link-left-nav">
            <%= link_to "Mes patients" , users_path%>

            <%= link_to "Mes rendez-vous", upcoming_reservations_path(current_user) %>

            <%#= link_to "Mon historique" , past_reservations_path(current_user) %>

            <%= link_to "Mon historique", profiles_path %>

            <%= link_to "Déconnexion", destroy_user_session_path, data: {turbo_method: :delete}, class: "dropdown-item" %>
          </div>
        </div>
      </div>
    </nav>
  </div>
</div>

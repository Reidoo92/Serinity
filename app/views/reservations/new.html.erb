
  <%= simple_form_for @reservation do |f|%>
<%= f.input :date,
            as: :string,
            input_html: { data: { controller: "datepicker" } } %>

    <%= link_to "Valider", root_path(@reservation), class: "new-button"%>

  <% end %>
